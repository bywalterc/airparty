<%= simple_form_for(@space) do |f| %>
  <!-- [...] -->
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.input :category %>
  <%= f.input :capacity %>
  <%= f.input :space_photo %>
  <%= f.submit %>
  <!-- [...] -->
<% end %>
